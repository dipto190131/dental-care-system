[build]
providers = ["nodejs", "python"]

[build.nodejs]
buildCommand = "npm run build"

[build.python]
pythonVersion = "3.11"
installCommand = "pip install -r requirements.txt"

[env]
PORT = "5000"
NODE_ENV = "production"
